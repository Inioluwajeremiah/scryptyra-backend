# ── Server ──────────────────────────────────────────────
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:3000

# ── MongoDB ──────────────────────────────────────────────
# MONGO_URI=mongodb+srv://ephphathagc_db_user:m2nyE2sTblnU1nLz@cluster0.waaldrq.mongodb.net/scryptyra
# Production example:
# MONGO_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/fadein?retryWrites=true&w=majority

# ── JWT ──────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=7d
JWT_COOKIE_EXPIRES_DAYS=7

# ── Email (Nodemailer) ───────────────────────────────────
# Option 1: Gmail with App Password (2FA must be on)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=

# Option 2: Mailtrap (for development/testing)
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=your_mailtrap_user
# SMTP_PASS=your_mailtrap_pass

EMAIL_FROM_NAME=Scryptera
EMAIL_FROM_ADDRESS=openhorizoninnovations@gmail.com

# ── Frontend URL (for email links) ───────────────────────
APP_URL=http://localhost:3000

# ── Stripe ───────────────────────────────────────────────
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs — create products in Stripe dashboard first
# Product: "Scryptyra Writer" → create 2 prices (monthly $12, annual $96)
STRIPE_PRICE_WRITER_MONTHLY=price_...
STRIPE_PRICE_WRITER_ANNUAL=price_...

# Product: "Scryptyra Pro" → create 2 prices (monthly $29, annual $228)
STRIPE_PRICE_PRO_MONTHLY=price_...
STRIPE_PRICE_PRO_ANNUAL=price_...

# Product: "Scryptyra Studio" → create 2 prices (monthly $79, annual $624)
STRIPE_PRICE_STUDIO_MONTHLY=price_...
STRIPE_PRICE_STUDIO_ANNUAL=price_...
